Read CLAUDE.md. Build UniMatch v2: outcome data, richer filters, compare, shareable links. Design language unchanged (dark navy, indigo→cyan).

The Supabase table public.unimatch_universities gained columns: control text ('Public'|'Private nonprofit'|'Private for-profit'), size_bucket text ('Small'|'Medium'|'Large'), undergrads int, setting_type text ('City'|'Suburb'|'Town'|'Rural'), grad_rate numeric 0-1, retention_rate numeric, median_earnings_10yr int, median_debt int, test_policy text ('Required'|'Optional'|'Not considered'), flags text[] (values like 'HBCU','HSI','Tribal','Women only','Men only','Religious affiliation'), sat_avg int. These are populated for US (source='scorecard') rows; mostly null for curated international rows — UI must degrade gracefully (hide missing stats, never show "null").

New table public.unimatch_programs (public read): university_id text, field_tag text (our canonical tags), cip_title text, credential text, median_earnings_5yr int, median_earnings_1yr int, median_debt int. One row per school per field tag (representative bachelor's program).

Build:
1. queryUniversities.js: extend fetchCandidates filters: control, sizeBucket, settingType, state (US 2-letter), testPolicy, minGradRate, flags (contains), keep existing region/country/cost/gpa logic. Also select the new columns and map to camelCase. New fetchProgramsFor(ids, fieldTags): one query .in('university_id', ids).in('field_tag', tags) returning a map; call it AFTER matching for only the displayed page of results (keep it to <=60 ids per call).
2. FilterBar v2: reorganize into "Essentials" (region chips, country, sliders, reach toggle) always visible + collapsible "More filters" section: Type (Public/Private nonprofit/For-profit chips), Size (S/M/L), Setting, US State (select, only when country/United States or region North America involved — always showing it is fine too), Test policy, Min graduation rate (slider 0-100%), Special mission checkboxes (HBCU, HSI, Women only). Active-filter count badge on the section toggle.
3. UniversityCard v2: add an "Outcomes" strip when data exists: grad rate %, median earnings 10yr ($Xk), median debt ($Xk); a small line for the matched program when program data exists for one of the student's selected interests: "★ {cip_title}: ${earnings_5yr} median 5yr". Type/size/setting as small muted meta text. Keep the card compact — the strip is one row of 2-3 stat chips.
4. Compare/shortlist: heart/+ button on each card saves to localStorage; a floating "Compared (n)" button opens an overlay comparing saved schools side by side (name, GPA needed, label, tuition, total, grad rate, earnings, debt, program earnings). Cap 4. Persist across visits.
5. Shareable links: serialize profile+filters into URL query params on search; on load, if params present, hydrate the form and auto-run the search. Add a "Share results" button that copies the URL (navigator.clipboard, with fallback).
6. Results: show "Showing X of Y matches" and a "Load more" that reveals more of the already-fetched results in steps of 30 (no re-query).
7. Admission odds hint on card badge tooltip: title attribute like "Acceptance rate 12% · your GPA is +0.3 above typical minimum".
8. Empty/loading states keep working. Mobile: new filters live inside the existing bottom sheet.
9. npm run build green; extend scripts/test-match.mjs with a few assertions for the new URL-param serialize/parse helpers (put them in src/lib/shareLink.js so they're testable in node). npm run test green.
